- defaults:
    link:
      relink: true

- shell:
    - [sudo chsh -s $(which zsh) root, Making zsh the default shell]
    - [curl -sfL git.io/antibody | sudo sh -s - -b /usr/local/bin, Install antibody]

- clean:
  - "/root"

- link:
    /root/.dotfiles: ./
    /root/.zshrc: .zshrc
    /root/.vimrc: .vimrc
